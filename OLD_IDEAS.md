# Ideas
Старые идеи которые были во время написания фреймворка.
Не нужно на них ориентироваться, оставляю для истории

## Bugs ##
 - api success не принимает string/int - сделать какой-то processor
 - [ ] __construct(?class $foo = null) не работает если явно не указать inject
 - [ ] проверить - как работает inject в конструкторе - ведь в контейнере берется только inject value
 - СТАНДАРТИЗИРОВАТЬ INJECT ВНУТРИ: дублирование кода в контейнере и контроллер-прокси (может еще где-то не знаю)
 - ПЛЮС - сделать попытку автотрансформации когда в класс когда мы делаем injectValue а там строка (как сейчас enum работает - должно работать через новый helper)
 - есть дублирование логики - ControllerProxy::__call принимает на вход аргументы функции, но используется это только в cli. нужно сделать injectCli, а это убрать
## Переработки по логированиям ##
 - [ ] Inject Exception ( + разделить ошибку когда нет аргумента и когда нет класса)
 - [ ] 500я когда делаем inject а поля такого нет в функции

## Daemon ##
 - [ ] сделать логирование успешных запросов
 - [ ] breaking change: сделать демона extends CliController и переиспользовать логирование и тд (все равно будет дубль с controller proxy)

## Идеи 2023 ##
1. Выровнять сигнатуры функции error в AbstractCliController и AbstractApiController, сейчас они сильно разные
2. Подумать над внедрением глобального PSR логгера на уровне app.
3. __missedRequiredArgument поведение функции не консистентно в разных контроллерах (api/cli/web)
4. Сделать настройки profiler
5. возвращать из abstractService типизированные данные (есть риск что будет очень долго, дешевле на grpc какой-нибудь перейти)


## Актуальные todo для lbaf и testsite по компонентам

### Composer & docker
1. Сделать composer create project

### Profiler
1. Сделать работу profiler глобально настраиваемой через $app. Внедрить туда какой-нибудь psr. Разобраться почему он медленный такой (см cli/stat compare)

### Db
1. Отрефакторить Lbaf\Database\MySql. Возможно внедрить туда profiler.
2. Сделать select($table, array|string $where)
3. Сейчас в result(...) нельзя использовать объект у которого переменные заданы через конструктор (да и вообще объект с какими-то обязательными полями в конструкторе) - не уверен что есть производительное решение
4. Сейчас в result/insert/update(...) нельзя использовать объект у которого тип свойств не поддерживается sql (например enum) - возможное решение - делать обертку внутри db и использовать код из ReflectionClassCreator::createClassFromData но возможно это будет ну очень медленно
### Controller
1. ~~Подумать про какие-то Request/Response объекты~~ Request объекты хорошо себя показали
2. Возвращать из функции контроллера Response объект, который выводить уже на уровень выше (?подумать что дает - как минимум для api более явное поведение + легче functional тесты писать)
3. Решить нужен ли в Controller $app. Можно просто все вокруг doBackground перенести в ControllerPrototype
4. Типизировать какие ошибки прерывают flow приложения, какие нет
6. Перехватывать целиком вывод контроллера, - чтобы минимизировать ошибку вывода перед $this->display Warning: Cannot modify header information - headers already sent
7. Подумать где должен быть все-таки Packer - в ApiController или в App? По новой логике - все-таки в ApiController
8. AbstractApiController нуждается в фиксах, например нет нормального логирования

### Container & Inject
1. Убрать использование PSR контейнера, оно тянет за собой кучу мусора
2. Сейчас из InjectException показывается название переменной из конструктора. Хочется переопределять это поведение для InjectValueArrayAbstract - чтобы показывать наружу красиво чего не хватает
2. Устранить дублирование кода в container / ReflectionHelper / ControllerProxy / ReflectionClassCreator
4. Сейчас есть неявное поведение: во всех местах кроме конструктора контроллера и вызываемых по цепочке классов нужно прописывать Inject, а в конструкторе только если это какой-то не стандартный Inject. Возможно стоит убрать логику автоматического заполнения конструктора

### Router
1. Сделать нормальное дефолтное поведение если route не найден
2. Написать описания к роутерам
3. Закрыть в роутерах доступ к папке LBAF, Vendor, Cli, etc (не уверен что это нужно)
4. Выпилить мусорные не используемые роутеры

### Легаси
1. Переписать \LBAF\Helper\apierror - сейчас вообще не работает 
2. Привести код к PSR-12 https://www.php-fig.org/psr/psr-12/ Отсмотреть все функции и классы на единый нейминг camelCase
3. Просмотреть весь код на предмет todo и вынести их сюда

### php 8.1
1. enums
2. nullsafe
3. named arguments
4. Property Promotion (e.g in MySqlConfig)
6. types: mixed return (db?), union, etc

### Big stories
1. Внедрить forks (как helper или App - подумать)
2. Подумать про запуск крон как части lbaf (какой-то единый скрипт который стоит в кроне `* * * * *` и откуда-то берет список конкретных скриптов для запуска)
3. Подумать над оформлением редко используемых частей lbaf как отдельных пакетов composer (например mysql/redis/rabbit/daemon/curl): очень криво, нужно выделять их в отдельный namespace иначе не работает загрузка.
4. Решить что-нибудь насчет csfixer
5. Внедрить свой db version manager в миниатюре (обычно в проектах используем phinx но у него есть фатальный недостаток)

